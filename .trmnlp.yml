# TRMNLP configuration
# {{ env.VARIABLE }} interpolation is available here
---
# auto-reload when files change (`watch: false` to disable)
watch:
  - .trmnlp.yml
  - src

# values of custom fields (defined in src/settings.yml)
custom_fields: {
  uptimerobot_api_key: "{{ env.UPTIMEROBOT_API_KEY }}"
}

# override variables
variables:
  trmnl: {
    plugin_settings: {
      instance_name: UptimeRobot
    }
  }
  # monitors:
  #   - friendly_name: Example Website 1
  #     url: https://site1.example.com
  #     status: 2
  #   - friendly_name: Example Website 2
  #     url: https://site2.example.com
  #     status: 0
  #   - friendly_name: Example Website 3
  #     url: https://site3.example.com
  #     status: 9
